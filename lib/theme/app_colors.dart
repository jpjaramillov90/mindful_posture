import 'package:flutter/material.dart';

class AppColors {
  // Color primario — Aqua Egeo
  static const Color primary = Color(0xFF5B99A8);

  // Color secundario — Verde Salvia Suave
  static const Color secondary = Color(0xFFA8BFA3);

  // Fondo neutro — Off-White
  static const Color background = Color(0xFFF7F9F5);

  // Sombras y grises suaves
  static const Color softGrey = Color(0xFF8A8A8A);
  static const Color darkText = Color(0xFF333333);
}
